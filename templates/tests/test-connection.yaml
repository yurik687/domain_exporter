apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "prometheus-domain-exporter.fullname" . }}-test-connection"
  labels:
    {{- include "prometheus-domain-exporter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "prometheus-domain-exporter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
